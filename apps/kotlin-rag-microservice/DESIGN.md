# Kotlin RAG microservice design

S2 — Offline in-memory corpus ingest/query in Kotlin on Boot 4.1.1.

## Endpoints

POST `/api/v1/ingest` · POST `/api/v1/query` · GET `/actuator/health`

## Out of scope

Paid embeddings/LLM in default tests. Cloud deploy.
