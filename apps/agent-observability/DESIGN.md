# Agent observability design

P5 — Record trajectories, cluster failures, propose skill/prompt deltas (offline).

## Stack pin

Boot 3.3.3 (AI 2.0 later).

## Endpoints

POST /api/v1/trajectories · GET /api/v1/trajectories · POST /api/v1/analyze · health
