app:
  security:
    api-key: test-spec-orchestrator-api-key-for-unit-tests-only

spring:
  ai:
    model:
      chat: ${SPRING_AI_MODEL_CHAT:none}
    openai:
      api-key: ${OPENAI_API_KEY:}
  main:
    banner-mode: "off"
