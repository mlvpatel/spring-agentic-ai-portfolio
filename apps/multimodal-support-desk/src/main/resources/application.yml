server:
  port: 8091

spring:
  ai:
    model:
      chat: ${SPRING_AI_MODEL_CHAT:none}
    openai:
      api-key: ${OPENAI_API_KEY:}
  application:
    name: multimodal-support-desk

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized
  info:
    env:
      enabled: false

info:
  app:
    name: multimodal-support-desk

app:
  security:
    api-key: ${MULTIMODAL_DESK_API_KEY:}
