# System architect design

P9 — Requirements → C4 Mermaid + ADR + capacity + parallel review notes (offline corpus).

## Stack pin

Boot 3.3.3 (AI 2.0 later). No live PGVector/Structurizr server.

## Endpoints

POST /api/v1/architect · health
