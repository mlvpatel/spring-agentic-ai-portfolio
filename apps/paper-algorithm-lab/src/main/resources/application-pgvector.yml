spring:
  datasource:
    url: ${PAPER_LAB_JDBC_URL:jdbc:postgresql://localhost:5434/papers}
    username: ${PAPER_LAB_JDBC_USER:postgres}
    password: ${PAPER_LAB_JDBC_PASSWORD:papers}
    driver-class-name: org.postgresql.Driver
