## Summary

What does this PR change, and why?

## Checklist

- [ ] `./mvnw test` passes locally (no API key required for the default suite)
- [ ] No real secrets, `.env` values, `*.p12`, or `*.pem` files committed
- [ ] Docs updated if behavior or setup changed
- [ ] Scoped to one concern when practical

## Test plan

How did you verify this (commands, endpoints, offline vs live AI)?

## Related issues

Fixes #
